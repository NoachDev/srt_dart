name: srt_dart
description: Bindings of Secure Reliable Transport (SRT) protocol for Dart.
version: 1.0.0
repository: https://github.com/NoachDev/srt_dart

environment:
  sdk: ^3.10.7

# Add regular dependencies here.
dependencies:
  ffigen: ^20.1.1
  ffi: ^2.1.5
  path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
